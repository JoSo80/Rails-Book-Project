<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title><%= yield(:html_title) + " / " if content_for?(:html_title) %>Top Rated Books</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.9.1/css/bulma.min.css">
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <section class="section">
      <div class="container">
        <h1 class="title is-1"><%= link_to 'Top Rated Books From Goodreads', root_path %></h1>

        <%= form_with(url: search_books_path, method: :get, local: true) do %>
          <div class= "field is-grouped mb-5">
            <div class= "control is-expanded">
              <%= text_field_tag(:keywords, nil, class: "input", placeholder: 'Search For Books')%>
            </div>
          <div class= "contorl">
            <%= submit_tag("Search", class: "button is-primary")%>
          </div>
        </div>
        <% end %>

        <%= yield %>

        <%= render partial: 'common/footer' %>
      </div>
    </section>
  </body>
</html>
